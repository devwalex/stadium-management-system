<!DOCTYPE html>
<html lang="en">
  @include('modules.dashboard.header')
  <body id="page-top">
    @!section('contentAuth')
        <!-- Page Wrapper -->
        <div id="wrapper">
    @include('modules.dashboard.sidebar')

           <!-- Content Wrapper -->
           <div id="content-wrapper" class="d-flex flex-column">

            <!-- Main Content -->
            <div id="content">
    @include('modules.dashboard.topbar')


    @!section('content')
            </div>
    @include('modules.dashboard.footer')
  </div>
        </div>

          <!-- Scroll to Top Button-->
    <a class="scroll-to-top rounded" href="#page-top">
      <i class="fas fa-angle-up"></i>
  </a>
  @include('modules.dashboard.footer-script')

  </body>

  </html>